{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Lancer le serveur",
            "program": "${workspaceFolder}/vigameurs-backend/server.js",
            "preLaunchTask": "npm install",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}
